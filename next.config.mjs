/** @type {import('next').NextConfig} */
const nextConfig = {
  // Turbopack is not enabled by default, so we can leave this empty or use valid options
  experimental: {
    // Just remove turbo unless you're using a valid object
  },
}

export default nextConfig
