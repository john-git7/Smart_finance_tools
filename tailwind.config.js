/** @type {import('tailwindcss').Config} */
const config = {
  darkMode: 'media', // Enables manual class-based dark mode
  content: [
    './src/**/*.{js,ts,jsx,tsx}', // Adjust if needed
  ],
  theme: {
    extend: {},
  },
  plugins: [],
};

export default config;
  a